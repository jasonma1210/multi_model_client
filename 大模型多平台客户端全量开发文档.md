大模型多平台客户端全量开发文档

**文档归属**：产品设计总监牵头全链路闭环设计

**适用平台**：iOS、Android 移动端（预留 Windows/macOS 桌面端扩展能力）

**核心参考**：深度对齐 Cherry Studio 核心产品能力，叠加全模态交互、强记忆体系、插件化扩展

**更新日期**：2026 年 04 月

**文档组成**：软件开发需求说明书 (SRS) + 产品全链路闭环设计 + 架构升级与技术实现方案 + 全流程研发落地计划

------

## 第一部分 软件开发需求说明书 (SRS)

### 1. 引言

#### 1.1 文档目的

本文档为大模型多平台客户端的官方需求基准文档，明确产品的功能边界、性能指标、安全合规要求、验收标准，作为产品设计、研发、测试、验收的唯一依据，确保所有参与方对产品需求有统一、无歧义的认知。

#### 1.2 产品范围

本产品是一款**多模型兼容、多会话强隔离、全模态交互、强记忆存储、可扩展插件化**的一站式大模型客户端，支持 iOS/Android 双端，覆盖本地开源模型与云端商用模型，提供文本对话、实时语音、视频理解、智能内容总结、RAG 知识库、Skills/MCP 插件扩展等核心能力，核心对标 Cherry Studio 的桌面端核心体验，并针对移动端场景做深度优化。

#### 1.3 术语定义

| 术语          | 全称                                                   | 定义                                                         |
| :------------ | :----------------------------------------------------- | :----------------------------------------------------------- |
| 会话隔离      | Session Isolation                                      | 核心产品特性，每个会话拥有独立的运行空间，绑定专属模型、系统提示词、上下文、配置，与其他会话无任何上下文、数据关联 |
| SYSTEM_PROMPT | 系统提示词                                             | 针对大模型的人设、行为规则、输出规范的指令，分为全局默认与会话级专属，会话级优先级最高 |
| MCP           | Model Context Protocol                                 | 大模型上下文协议，标准化的工具与上下文接入规范               |
| RAG           | Retrieval Augmented Generation                         | 检索增强生成，通过知识库 / 外部内容补充大模型上下文          |
| ASR           | Automatic Speech Recognition                           | 自动语音识别，音频转文本                                     |
| TTS           | Text To Speech                                         | 语音合成，文本转音频                                         |
| VAD           | Voice Activity Detection                               | 语音活动检测，识别音频流中的有效语音片段                     |
| gguf          | 主流端侧大模型量化格式，跨平台兼容性强，移动端适配成熟 |                                                              |

#### 1.4 参考文档

1. Cherry Studio 官方产品文档与功能规范
2. llama.cpp/Whisper.cpp 官方开发文档
3. MCP 1.0 官方协议规范
4. iOS/Android 平台应用开发与安全合规规范
5. 《个人信息保护法》《网络安全法》相关合规要求

------

### 2. 产品总体描述

#### 2.1 产品定位

一款**本地优先、云端兼容、会话绝对隔离、人设自由定义、全模态交互、强记忆体系、可无限扩展**的移动端大模型客户端，解决用户多模型切换繁琐、上下文串扰、多场景人设不统一、内容总结效率低、隐私数据泄露等核心痛点，打造移动端一站式大模型交互入口。

#### 2.2 目标用户

1. 核心用户：AI 爱好者、技术从业者、内容创作者、办公人群
2. 次要用户：学生群体、普通日常使用用户

#### 2.3 运行环境

| 平台    | 最低版本要求  | 硬件要求                                                    |
| :------ | :------------ | :---------------------------------------------------------- |
| iOS     | iOS 15.0+     | iPhone 8 及以上机型，支持 Metal 加速，最低 4GB 内存         |
| Android | Android 10.0+ | 支持 arm64-v8a 架构，最低 4GB 内存，支持 OpenCL/Vulkan 加速 |

#### 2.4 约束条件

1. 隐私约束：用户本地数据默认仅存储在设备本地，严禁未经用户许可上传用户隐私数据
2. 性能约束：移动端端侧推理必须适配低内存场景，严禁频繁出现 OOM、闪退
3. 合规约束：必须符合应用商店审核规范，严禁违规收集用户信息，严禁提供违规内容生成能力
4. 开源约束：所有使用的开源组件必须严格遵守对应开源协议，标注版权信息
5. 兼容性约束：核心能力必须在双端保持一致，平台专属能力需做原生适配

------

### 3. 核心功能需求

#### 3.1 多模型管理模块（对齐 Cherry Studio 核心能力）

| 功能点       | 详细需求                                                     |
| :----------- | :----------------------------------------------------------- |
| 本地模型支持 | 1. 支持 gguf 格式主流开源模型，包括纯文本 LLM、多模态模型<br />2. 提供模型市场，内置主流模型下载地址，支持断点续传、哈希校验<br />3. 自动解析模型元数据，识别模型类型（纯文本 / 多模态）、参数量、量化级别、上下文窗口、能力边界<br />4. 模型生命周期管理：下载、加载、卸载、删除、参数配置，支持后台加载 |
| 远程模型支持 | 1. 兼容所有 OpenAI 格式 API，内置主流商用模型预设（GPT 系列、Claude 系列、通义千问、文心一言、豆包等）<br />2. 支持用户自定义远程模型接入，配置 API 地址、密钥、模型参数、能力标签<br />3. 自动探测远程模型能力，识别文本 / 多模态类型、工具调用支持、上下文窗口 |
| 模型参数配置 | 1. 全局默认参数配置：温度、top_p、top_k、最大输出 token、频率惩罚等<br />2. 支持会话级参数覆盖，每个会话可单独配置模型推理参数 |

#### 3.2 多会话隔离与管理模块（核心新增，对齐 Cherry Studio）

| 功能点           | 详细需求                                                     |
| :--------------- | :----------------------------------------------------------- |
| 会话绝对隔离     | 1. 每个会话为独立运行空间，绑定专属大模型，会话之间无任何上下文关联、无数据交叉<br />2. 每个会话的对话历史、系统提示词、模型配置、参数设置、RAG 知识库、技能开关完全独立<br />3. 会话切换时，自动卸载上一会话的模型上下文，加载当前会话的专属配置，严禁上下文串扰 |
| 会话生命周期管理 | 1. 支持新建会话、删除会话、重命名会话、置顶会话、归档会话<br />2. 支持会话文件夹分类管理，用户可创建文件夹，对会话进行分组归类<br />3. 支持会话复制、导出，可将会话内容导出为 Markdown/PDF 格式 |
| 会话快速启动     | 1. 提供会话模板功能，用户可保存预设模板（含绑定模型、系统提示词、参数配置、RAG 绑定）<br />2. 一键基于模板创建新会话，无需重复配置 |

#### 3.3 系统提示词 (SYSTEM_PROMPT) 管理模块

| 功能点         | 详细需求                                                     |
| :------------- | :----------------------------------------------------------- |
| 分级提示词管理 | 1. 全局默认系统提示词：用户可设置全局默认人设，未单独配置的会话默认使用<br />2. 会话级专属系统提示词：每个会话可单独设置自定义提示词，优先级高于全局默认<br />3. 提示词实时生效：修改会话提示词后，新一轮对话立即生效，不影响历史上下文 |
| 提示词模板库   | 1. 内置高频人设模板（如傲娇女王、技术专家、翻译官、文案顾问、代码助手等）<br />2. 支持用户自定义创建、保存、编辑、删除提示词模板，一键应用到会话<br />3. 支持提示词变量，可插入用户信息、当前时间、会话信息等动态变量 |

#### 3.4 智能内容总结引擎（核心新增）

| 功能点                | 详细需求                                                     |
| :-------------------- | :----------------------------------------------------------- |
| 网页一键总结          | 1. 支持用户在会话内输入任意网页 URL，自动触发网页内容解析、提取、总结2. 支持系统分享扩展：用户在浏览器、其他 APP 中分享网页到本客户端，自动新建会话并完成总结3. 自动过滤网页广告、无关内容，提取核心正文、标题、关键信息，基于用户需求生成总结 |
| 主流内容平台 URL 总结 | 1. 支持平台：抖音、小红书、Bilibili、微信视频号、知乎、微博2. 抖音 / 视频号：自动解析短链接，提取视频标题、文案、自动生成的字幕，无字幕视频自动提取音频并通过 ASR 转写文本，完成内容总结3. 小红书：提取笔记标题、正文、图片 OCR 文本、评论核心信息，生成总结4. B 站：提取视频标题、简介、字幕、弹幕核心信息、分 P 内容，生成总结5. 知乎 / 微博：提取正文、高赞评论核心信息，生成总结 |
| 总结能力配置          | 1. 支持多种总结模式：极简摘要、详细总结、要点提炼、思维导图大纲、观点提炼2. 总结内容自动注入当前会话上下文，支持用户基于总结内容继续追问 |

#### 3.5 强记忆 Memory 引擎（核心偏向）

| 功能点         | 详细需求                                                     |
| :------------- | :----------------------------------------------------------- |
| 分层记忆架构   | 1. 瞬时记忆：当前轮对话输入输出，token 窗口内实时保留2. 工作记忆：当前会话专属上下文，滑动窗口 + 动态摘要，严格隔离，不跨会话共享3. 长时记忆：分为全局长时记忆与会话专属长时记忆，用户可开关会话是否接入全局记忆4. 归档记忆：长期未使用的低权重记忆，冷存储，按需召回 |
| 记忆全流程管理 | 1. 自动提取：每轮对话结束后，自动从对话中提取实体、事实、用户偏好，生成标准化记忆条目2. 记忆检索：用户输入 Query 后，自动检索当前会话专属记忆 + 可选全局记忆，注入 Prompt3. 记忆更新与衰减：记忆被调用时提升权重，长期未访问自动衰减、归档4. 手动管理：支持用户手动编辑、删除、置顶、归档记忆，自定义记忆权重 |
| 存储隔离       | 会话专属记忆仅当前会话可访问，全局记忆所有会话可按需接入，严格遵循用户配置 |

#### 3.6 RAG 检索增强引擎

| 功能点     | 详细需求                                                     |
| :--------- | :----------------------------------------------------------- |
| 知识库管理 | 1. 支持用户创建全局知识库与会话专属知识库，会话专属知识库仅当前会话可访问2. 支持文档格式：PDF、Word、TXT、Markdown、Excel、图片、音频、视频3. 自动解析文档内容，图片通过 OCR 提取文本，音视频通过 ASR 转写文本，完成语义分块、向量化、存储 |
| 检索增强   | 1. 对话时自动检索当前会话绑定的知识库 + 可选全局知识库，召回相关内容注入 Prompt2. 支持手动选择知识库片段，强制注入对话上下文3. 与 URL 总结引擎联动，URL 解析后的内容可一键存入知识库，供后续检索使用 |

#### 3.7 Skills 插件与 MCP 兼容引擎

| 功能点       | 详细需求                                                     |
| :----------- | :----------------------------------------------------------- |
| 统一工具调度 | 兼容 OpenAI Function Calling、Anthropic Tool Use、MCP 1.0 协议，实现原生技能、自定义技能、MCP 服务的统一管理与调用 |
| 分级技能管理 | 1. 全局技能开关：设置全局可用的技能 / MCP 服务2. 会话级技能开关：每个会话可单独开启 / 关闭指定技能 / MCP 服务，独立配置，互不影响 |
| 技能类型     | 1. 原生内置技能：计算器、单位转换、日历、天气、文件管理、URL 解析、设备信息等2. 自定义技能：用户通过 JSON Schema 定义参数，JavaScript 编写执行逻辑，沙箱隔离运行3. MCP 服务：完整兼容 MCP 协议，支持 STDIO/HTTP/WebSocket 传输，用户可添加自定义 MCP 服务 |

#### 3.8 全模态音视频交互模块

| 功能点       | 详细需求                                                     |
| :----------- | :----------------------------------------------------------- |
| ASR 语音识别 | 1. 本地核心：Whisper.cpp 量化模型，离线可用，多语言支持2. 平台原生：iOS Speech/Android SpeechRecognizer3. 云端兼容：主流商用 ASR API4. 支持流式实时转写，支持方言识别 |
| TTS 语音合成 | 1. 本地核心：Piper TTS 移动端移植，离线可用2. 平台原生：iOS AVSpeechSynthesizer/Android TextToSpeech3. 云端兼容：主流商用 TTS API4. 支持流式合成，边生成边播报，支持音色、语速、语调配置 |
| 实时语音对话 | 1. 低延迟全链路流式交互，VAD 自动检测语音起止，支持打断、连续对话2. 支持语音唤醒，免手动触发输入3. 会话级语音配置，每个会话可单独设置 ASR/TTS 引擎、音色、语速 |
| 视频理解能力 | 1. 仅多模态模型可用，支持本地视频文件理解、实时摄像头视频对话2. 自动提取视频关键帧，完成视觉理解，支持连续多轮对话3. 会话级能力开关，单独配置是否启用视频理解 |

#### 3.9 系统与设置模块

| 功能点   | 详细需求                                                     |
| :------- | :----------------------------------------------------------- |
| 数据管理 | 1. 支持一键导出所有用户数据、一键清空本地数据2. 支持会话、记忆、知识库的备份与恢复3. 加密存储所有用户数据，支持设置应用锁（密码 / 生物识别） |
| 界面配置 | 1. 支持深色 / 浅色模式、主题色自定义2. 支持对话界面字体、字号、行高配置3. 支持 markdown 渲染、代码高亮、代码复制 |
| 权限管理 | 1. 最小权限原则，仅申请必要的麦克风、摄像头、存储权限2. 权限使用记录可查，支持随时关闭权限 |

------

### 4. 非功能需求

#### 4.1 性能需求

1. 冷启动速度：双端冷启动到进入主界面时间≤2 秒
2. 模型加载速度：7B 参数 4bit 量化模型加载时间≤3 秒，14B 参数 4bit 量化模型加载时间≤8 秒
3. 推理速度：本地 7B 模型端侧推理速度≥30 token / 秒，云端模型流式响应首包延迟≤500ms
4. 语音对话端到端延迟：全链路流式交互延迟≤500ms
5. 内存占用：正常对话场景内存占用≤1GB，模型加载后峰值内存占用≤设备可用内存的 60%，严禁频繁 OOM、闪退
6. 功耗：连续 1 小时对话场景，设备温升≤10℃，无明显发烫

#### 4.2 兼容性需求

1. 系统兼容性：覆盖 iOS 15.0+、Android 10.0+，系统版本覆盖率≥95%
2. 机型兼容性：适配主流品牌旗舰机、中端机、入门机，主流机型适配率≥98%
3. 模型兼容性：支持 llama.cpp 兼容的所有 gguf 格式模型，主流开源模型适配率 100%
4. API 兼容性：兼容所有 OpenAI 格式的远程模型 API，主流商用模型预设覆盖率 100%

#### 4.3 安全性需求

1. 数据加密：所有用户本地数据（对话历史、记忆、知识库、文件）采用 AES-256 加密存储
2. 密钥安全：API 密钥、加密密钥存储在平台硬件级密钥链（iOS Keychain/Android Keystore），严禁明文存储
3. 隐私保护：所有用户数据默认仅存储在本地，仅用户使用云端服务时，才会发送必要内容到对应 API，严禁未经许可上传用户隐私数据
4. 沙箱隔离：自定义 JS 脚本在 QuickJS 沙箱中运行，限制系统资源访问，严禁恶意代码执行
5. 应用安全：Android 开启代码混淆与加固，iOS 开启符号剥离，核心库静态编译，防止逆向破解
6. 内容安全：内置敏感内容过滤机制，严禁生成违规违法内容

#### 4.4 可用性需求

1. 界面可用性：核心功能操作路径≤3 步，新手用户可快速上手
2. 稳定性：应用崩溃率≤0.2%，ANR 率≤0.1%
3. 离线可用性：本地模型 + 本地 ASR/TTS + 本地 RAG / 记忆，完全离线场景可正常使用所有核心功能
4. 可维护性：模块化架构，新增模型、技能、平台解析规则无需重构核心代码
5. 可扩展性：预留插件化接口，支持后续功能扩展

------

### 5. 接口需求

#### 5.1 内部模块接口

1. 会话管理接口：提供会话创建、删除、查询、配置更新、上下文加载 / 卸载接口
2. 模型管理接口：提供模型下载、加载、推理、卸载、参数配置、能力识别接口
3. 对话引擎接口：提供对话请求、流式响应、工具调用调度、上下文管理接口
4. 记忆引擎接口：提供记忆提取、存储、检索、更新、删除接口
5. RAG 引擎接口：提供文档解析、分块、向量化、存储、检索接口
6. URL 解析引擎接口：提供 URL 校验、平台识别、内容提取、文本转换接口
7. 音视频处理接口：提供音频采集 / 播放、VAD 检测、ASR 转写、TTS 合成、视频帧提取接口

#### 5.2 外部第三方接口

1. 云端大模型 API：兼容 OpenAI 格式、Anthropic、通义千问、文心一言、豆包等主流商用模型 API
2. 云端 ASR/TTS API：兼容阿里云、腾讯云、火山引擎、OpenAI 等主流商用音视频 API
3. 模型下载源：兼容 Hugging Face、ModelScope 等开源模型平台下载接口
4. 平台内容解析接口：适配抖音、小红书、B 站、视频号等平台的公开内容解析规则

------

### 6. 数据需求

#### 6.1 核心数据结构设计

| 数据表                             | 核心字段                                                     | 隔离规则                                                    |
| :--------------------------------- | :----------------------------------------------------------- | :---------------------------------------------------------- |
| 会话表 (session)                   | session_id、session_name、folder_id、model_id、system_prompt、model_params、create_time、update_time、is_pinned、is_archived | 主键 session_id，所有关联数据通过 session_id 隔离           |
| 对话历史表 (message)               | message_id、session_id、role、content、type、token_count、create_time、tool_call_info | 外键 session_id，仅当前会话可查询对应数据                   |
| 系统提示词模板表 (prompt_template) | template_id、template_name、template_content、create_time、update_time | 全局共享，支持会话一键引用                                  |
| 模型配置表 (model)                 | model_id、model_name、model_type、model_source、model_path、api_config、model_capability、model_params、create_time | 全局共享，会话绑定 model_id                                 |
| 记忆表 (memory)                    | memory_id、session_id、memory_type、content、entity_tags、weight、create_time、update_time、last_access_time、is_global、is_archived | session_id 为空则为全局记忆，非空则为会话专属记忆，严格隔离 |
| 知识库表 (knowledge_base)          | kb_id、kb_name、session_id、create_time、update_time、is_global | session_id 为空则为全局知识库，非空则为会话专属知识库       |
| 文档分块表 (chunk)                 | chunk_id、kb_id、content、vector、metadata、create_time      | 外键 kb_id，严格归属对应知识库                              |

#### 6.2 数据存储需求

1. 结构化数据：SQLite + drift ORM 存储，所有表通过 session_id 实现数据隔离
2. 向量数据：sqlite-vss 存储，与结构化数据共用存储，跨平台兼容
3. 文件数据：加密存储在应用沙盒内，按会话 / 知识库分类管理
4. 敏感数据：存储在平台硬件级密钥链，不写入普通存储

#### 6.3 数据备份与迁移需求

1. 支持全量数据备份与恢复，备份文件加密存储
2. 支持单会话、单知识库数据导出与导入
3. 支持跨设备数据迁移，预留多设备同步扩展能力

------

### 7. 验收标准

#### 7.1 功能验收标准

1. 所有需求文档中明确的功能点 100% 实现，无遗漏
2. 会话隔离功能验收：多会话同时绑定不同模型、不同系统提示词，对话上下文无任何串扰，数据完全隔离
3. 模型功能验收：本地模型加载、推理无异常，远程模型接入、调用无异常，模型类型识别准确率 100%
4. URL 总结功能验收：普通网页、抖音、小红书、B 站、视频号 URL 解析成功率≥95%，总结内容准确覆盖核心信息
5. 音视频功能验收：ASR 转写准确率≥95%，TTS 合成无异常，实时语音对话无明显延迟，支持打断
6. 记忆与 RAG 功能验收：记忆提取准确，检索召回率≥90%，RAG 文档解析准确，检索内容与 Query 相关度≥90%
7. 技能与 MCP 功能验收：原生技能、自定义技能、MCP 服务调用无异常，工具调用调度准确

#### 7.2 性能验收标准

1. 所有性能指标达到本文档 4.1 节的要求，无超标项
2. 连续 72 小时运行无闪退、无内存泄漏、无明显卡顿
3. 低内存机型运行无 OOM、无闪退

#### 7.3 兼容性验收标准

1. 双端系统版本覆盖达标，主流机型适配无异常
2. 主流开源模型、商用模型接入无异常
3. 不同分辨率、不同屏幕尺寸的设备界面适配无异常

#### 7.4 安全验收标准

1. 所有用户数据加密存储，无明文存储敏感信息
2. 权限申请符合平台规范，无违规收集用户信息
3. 渗透测试无高危漏洞，沙箱隔离有效，无恶意代码执行风险
4. 符合应用商店审核规范，无违规内容

------

## 第二部分 产品全链路闭环设计（产品设计总监视角）

### 1. 产品核心逻辑闭环

本产品以 **「会话」为核心最小单元 **，所有功能、数据、配置均围绕会话展开，形成「**模型配置→会话创建→人设定义→交互体验→内容沉淀→记忆存储→复用迭代**」的完整闭环，深度对齐 Cherry Studio 的核心产品哲学，同时针对移动端场景做了全链路优化，解决用户多场景、多模型、多人设的使用痛点。

### 2. 用户全旅程闭环设计

#### 2.1 新手引导阶段（首次启动）

1. 隐私合规告知：明确告知用户隐私保护原则，获取必要权限
2. 快速初始化：引导用户选择使用模式（本地优先 / 云端优先），一键下载推荐本地模型，或快速配置常用远程模型 API
3. 新手教程：通过引导式交互，让用户快速掌握「创建会话→选择模型→设置人设→发起对话」的核心流程

#### 2.2 核心使用阶段（日常交互）

1. 会话创建：用户可基于模板一键创建会话，或自定义配置会话的绑定模型、系统提示词、参数、知识库、技能开关
2. 多模态交互：用户可在会话内进行文本对话、语音对话、视频理解、URL 内容总结、工具调用，所有交互内容仅归属当前会话，与其他会话完全隔离
3. 内容沉淀：对话内容、URL 总结结果可一键存入知识库，对话中的核心信息自动提取为记忆，存储到会话专属记忆或全局记忆
4. 会话管理：用户可对会话进行分类、归档、导出、分享，高频使用会话可置顶，模板可保存复用

#### 2.3 高阶使用阶段（能力扩展）

1. 自定义扩展：用户可添加自定义远程模型、创建自定义提示词模板、开发自定义技能、接入第三方 MCP 服务
2. 知识库构建：用户可创建个人专属知识库，绑定到指定会话，实现专属领域的检索增强生成
3. 记忆体系优化：用户可手动编辑、管理记忆，设置记忆的权重、共享范围，打造专属的个人 AI 记忆体

#### 2.4 数据管理阶段（安全管控）

1. 数据备份：用户可一键备份全量数据，或导出指定会话、知识库数据
2. 隐私管控：用户可随时清空本地数据、关闭权限、设置应用锁，完全掌控自己的数据
3. 配置同步：预留多设备同步能力，用户可跨设备同步自己的模型配置、提示词模板、会话模板

### 3. 核心功能产品与技术协同闭环

#### 3.1 会话隔离闭环

- 产品逻辑：每个会话是独立的「AI 分身」，用户可同时拥有技术专家、文案顾问、生活助手等多个分身，互不干扰
- 技术实现：通过 session_id 实现全链路数据隔离，每个会话对应独立的对话引擎实例，切换会话时完全重置上下文，确保无串扰
- 闭环验证：会话切换时，模型、系统提示词、上下文、配置同步切换，历史对话无交叉，记忆检索严格遵循会话隔离规则

#### 3.2 系统提示词闭环

- 产品逻辑：用户可针对不同会话定义不同的人设，预设模板快速复用，修改实时生效
- 技术实现：分级提示词优先级管理，会话级提示词覆盖全局默认，对话请求时自动注入当前会话的专属提示词
- 闭环验证：修改会话提示词后，新一轮对话立即生效，不同会话的提示词互不影响，模板一键应用无异常

#### 3.3 URL 内容总结闭环

- 产品逻辑：用户看到任何有价值的内容，只需复制 URL 或分享到本客户端，即可一键完成总结，基于总结内容继续追问
- 技术实现：URL 自动识别→平台适配解析→内容提取 / 音视频转写→文本预处理→大模型总结→内容注入会话上下文→一键存入知识库
- 闭环验证：主流平台 URL 解析成功率达标，总结内容准确，可基于总结内容连续追问，内容可沉淀到知识库复用

#### 3.4 记忆与 RAG 闭环

- 产品逻辑：用户与 AI 的每一次对话，都会自动沉淀为记忆，上传的每一份文档都会成为知识库，AI 会越来越懂用户，回答越来越精准
- 技术实现：对话结束→自动提取记忆→向量化存储→对话触发检索→记忆 + RAG 内容注入 Prompt→大模型生成回答→记忆权重更新
- 闭环验证：记忆提取准确，检索召回精准，多轮对话后 AI 可记住用户的偏好与事实，会话专属记忆不跨会话泄露

------

## 第三部分 整体架构升级与技术实现方案

### 1. 架构升级总览

在 V1.0 分层架构的基础上，新增**会话管理中心**、**URL 内容解析引擎**、**平台适配解析模块**，重构对话引擎为**会话级独立实例架构**，实现全链路会话隔离，深度对齐 Cherry Studio 核心能力，整体架构如下：

```
┌─────────────────────────────────────────────────────────────────────┐
│                     UI交互层                                          │
│  会话列表/文件夹、对话界面、模型市场、记忆管理、知识库、技能市场    │
│  提示词模板库、设置界面、内容总结界面                                │
├─────────────────────────────────────────────────────────────────────┤
│                     业务能力层                                        │
│  会话管理中心、对话管理、模型管理、记忆管理、RAG管理、技能/MCP管理  │
│  系统提示词管理、URL内容总结管理、音视频交互管理                    │
├─────────────────────────────────────────────────────────────────────┤
│                     核心引擎层                                        │
│  会话实例调度器、对话引擎、模型推理引擎、Memory引擎、RAG引擎        │
│  插件引擎、MCP客户端、工具调用调度器、多模态处理引擎                │
│  URL内容解析引擎、平台适配解析模块                                   │
├─────────────────────────────────────────────────────────────────────┤
│                     基础能力层                                        │
│  存储引擎、音视频处理、网络请求、硬件加速、权限管理、加密模块        │
├─────────────────────────────────────────────────────────────────────┤
│                     跨平台适配层                                      │
│  Flutter/Dart FFI、平台原生API桥接、CPU/GPU架构适配、分享扩展适配   │
└─────────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────────┐
│         安全合规层（贯穿所有层级）                                   │
│  数据加密、权限管控、沙箱隔离、密钥安全、合规审计、内容安全过滤    │
└─────────────────────────────────────────────────────────────────────┘
```

### 2. 核心新增 / 升级模块详细实现方案

#### 2.1 会话管理中心（核心升级，对齐 Cherry Studio）

##### 2.1.1 核心设计目标

实现会话的全生命周期管理，保证会话之间的绝对隔离，每个会话对应一个独立的运行实例，所有数据、配置、上下文完全独立。

##### 2.1.2 会话实例数据结构

每个会话实例包含以下专属数据，完全通过 session_id 隔离：

```
class SessionInstance {
  // 基础信息
  final String sessionId;
  final String sessionName;
  final String? folderId;
  
  // 模型专属配置
  final String modelId;
  final ModelConfig modelConfig;
  final Map<String, dynamic> inferenceParams; // 温度、top_p等
  
  // 提示词专属配置
  final String systemPrompt;
  final bool useGlobalPrompt;
  
  // 上下文专属数据
  final List<Message> contextMessages;
  final int maxContextWindow;
  
  // 能力专属开关
  final List<String> enabledSkills;
  final List<String> enabledMcpServices;
  final List<String> boundKnowledgeBaseIds;
  final bool enableGlobalMemory;
  final bool enableVideoUnderstanding;
  
  // 音视频专属配置
  final AsrConfig asrConfig;
  final TtsConfig ttsConfig;
  
  // 状态信息
  final bool isPinned;
  final bool isArchived;
  final DateTime createTime;
  final DateTime updateTime;
}
```

##### 2.1.3 核心实现流程

1. **会话创建流程**

   ```
   用户触发新建会话 → 选择模板/自定义配置 → 生成唯一session_id
   → 初始化会话实例，绑定模型、系统提示词、参数配置、能力开关
   → 持久化会话数据到SQLite会话表 → 创建会话专属的上下文、记忆、知识库存储空间
   → 进入对话界面，完成会话创建
   ```

   

2. **会话切换流程**

   ```
   用户切换会话 → 暂停当前会话实例，保存上下文状态到本地
   → 卸载当前会话绑定的模型上下文（如需切换模型）
   → 加载目标会话的实例数据，恢复会话状态
   → 加载目标会话绑定的模型、系统提示词、上下文、配置
   → 渲染对话界面，完成会话切换，全程无上下文串扰
   ```

   

3. **会话隔离保障机制**

   

   - 所有数据库操作必须携带 session_id 过滤条件，严禁无 session_id 的全表查询
   - 每个会话对应独立的对话引擎实例，上下文内存空间完全隔离
   - 会话级记忆、知识库仅当前会话可访问，全局记忆需用户手动开启才会接入
   - 会话删除时，同步删除该会话对应的所有数据，包括对话历史、专属记忆、专属知识库、配置数据

   

#### 2.2 系统提示词 (SYSTEM_PROMPT) 管理模块

##### 2.2.1 分级优先级设计

1. 优先级规则：**会话级自定义系统提示词 > 会话引用的模板提示词 > 全局默认系统提示词**
2. 未单独配置提示词的会话，默认使用全局默认提示词，用户可随时切换为自定义模式
3. 提示词修改后，仅对新一轮对话生效，历史对话的上下文不受影响，保证对话连贯性

##### 2.2.2 模板库实现方案

1. 内置模板采用静态资源打包，随应用发布，支持用户一键还原默认模板
2. 用户自定义模板存储在 SQLite 模板表，支持导入、导出、分享
3. 模板支持动态变量，通过模板引擎解析，支持的变量包括：{{user_name}}、{{current_time}}、{{current_date}}、{{session_name}}、{{model_name}} 等
4. 对话请求时，自动解析模板中的变量，替换为实际值，注入到系统 Prompt 中

#### 2.3 URL 内容解析与智能总结引擎（核心新增）

##### 2.3.1 整体架构设计

采用**统一调度 + 平台适配插件化**架构，新增平台适配解析模块，支持快速新增平台解析规则，整体流程如下：

```
用户输入URL/系统分享URL → URL合法性校验 → 短链接还原 → 平台类型识别
→ 调度对应平台的解析插件 → 内容提取（文本/图片OCR/音视频ASR转写）
→ 内容预处理（过滤广告、无关内容、语义分块） → 构造总结Prompt
→ 送入当前会话绑定的大模型 → 流式生成总结内容 → 渲染到对话界面
→ 支持用户一键存入知识库/继续追问
```

##### 2.3.2 分平台解析实现方案

| 平台类型      | 核心解析实现                                                 |
| :------------ | :----------------------------------------------------------- |
| 普通网页      | 1. 模拟移动端 UA 发起请求，获取网页 HTML2. 使用 readability 算法提取核心正文，过滤广告、导航栏、评论区等无关内容3. 对正文进行语义分段，预处理后送入大模型 |
| 抖音 / 视频号 | 1. 短链接自动还原，获取真实视频 URL2. 提取视频标题、发布者文案、自动生成的字幕3. 无字幕视频：提取音频流，通过本地 Whisper.cpp ASR 转写为文本4. 合并所有文本内容，预处理后送入大模型 |
| 小红书        | 1. 解析笔记标题、正文内容2. 笔记图片通过 OCR 提取文本内容3. 提取高赞评论的核心信息4. 合并所有内容，预处理后送入大模型 |
| B 站          | 1. 解析视频标题、简介、分 P 信息2. 提取视频 CC 字幕、自动生成字幕3. 提取高赞弹幕、高赞评论的核心信息4. 合并所有内容，预处理后送入大模型 |
| 知乎 / 微博   | 1. 提取正文内容、发布者信息2. 提取高赞评论的核心观点3. 预处理后送入大模型 |

##### 2.3.3 系统分享扩展实现

1. iOS 端：开发 Share Extension，接收系统分享的 URL，自动唤醒主应用，新建会话并触发 URL 解析与总结
2. Android 端：配置 Intent Filter，接收其他应用分享的 URL，自动唤醒主应用，新建会话并触发 URL 解析与总结
3. 分享内容自动缓存，支持离线查看总结结果

#### 2.4 原有核心模块适配升级

##### 2.4.1 模型管理模块升级

1. 新增模型与会话的绑定关系，支持会话切换时自动切换对应模型
2. 优化模型预加载机制，高频使用的模型可常驻内存，提升会话切换速度
3. 新增模型能力标签体系，会话创建时自动过滤不符合需求的模型（如纯文本模型无法用于视频理解会话）

##### 2.4.2 记忆引擎升级

1. 新增会话专属记忆与全局记忆的分级存储，严格通过 session_id 隔离
2. 新增记忆共享开关，用户可选择当前会话是否接入全局记忆
3. 优化记忆提取逻辑，仅提取当前会话的有效信息，避免跨会话数据泄露

##### 2.4.3 RAG 引擎升级

1. 新增会话专属知识库，仅当前会话可访问与检索
2. 新增知识库与会话的绑定关系，对话时仅检索当前会话绑定的知识库 + 可选全局知识库
3. 与 URL 总结引擎联动，支持 URL 解析后的内容一键存入指定知识库

##### 2.4.4 对话引擎升级

1. 重构为会话级独立实例架构，每个会话对应一个独立的对话引擎实例
2. 对话请求时，自动注入当前会话的系统提示词、上下文、记忆、RAG 内容、可用技能
3. 优化流式响应处理，会话切换时自动终止上一会话的流式请求，避免数据串扰

##### 2.4.5 音视频模块升级

1. 新增会话级 ASR/TTS 配置，每个会话可单独设置引擎、音色、语速
2. 优化实时语音对话的打断机制，会话切换时自动终止上一会话的 TTS 播报与音频采集
3. 视频理解能力新增会话级开关，仅多模态模型绑定的会话可启用

------

## 第四部分 整体实现流程

### 1. 项目研发全流程（总周期 12 周，分 6 个阶段）

| 阶段                   | 周期        | 核心工作                                                     | 交付物                                                     | 责任人                                         |
| :--------------------- | :---------- | :----------------------------------------------------------- | :--------------------------------------------------------- | :--------------------------------------------- |
| 需求评审与架构设计阶段 | 第 1-2 周   | 1. 需求文档评审，确认需求边界与验收标准2. 产品原型设计与评审3. 技术架构设计与评审4. 技术选型确认，开发环境搭建5. 任务拆解与排期 | 需求终稿、产品原型、架构设计文档、技术选型文档、项目排期表 | 产品设计总监、技术负责人、架构师               |
| 核心底层模块开发阶段   | 第 3-4 周   | 1. 跨平台框架搭建，基础能力层开发2. 存储引擎、加密模块、权限管理开发3. llama.cpp/Whisper.cpp 跨平台移植与适配4. 模型推理引擎、音视频处理引擎开发5. 核心接口定义与开发 | 跨平台基础框架、底层引擎 SDK、核心接口文档、单元测试用例   | 原生开发工程师、Flutter 开发工程师、算法工程师 |
| 核心业务模块开发阶段   | 第 5-8 周   | 1. 会话管理中心、对话引擎开发2. 模型管理模块、系统提示词模块开发3. 记忆引擎、RAG 引擎开发4. URL 内容解析与智能总结引擎开发5. Skills 插件与 MCP 兼容引擎开发6. 多模态音视频交互模块开发 | 核心功能模块代码、集成测试用例、功能演示 Demo              | 全栈开发工程师、算法工程师、测试工程师         |
| UI 界面开发与联调阶段  | 第 7-9 周   | 1. UI 界面开发，适配双端不同机型2. 前后端联调，所有功能模块对接3. 新手引导、交互体验优化4. 系统分享扩展开发与适配 | 完整可运行的应用安装包、UI 适配报告、交互体验优化报告      | Flutter 开发工程师、UI 设计师、产品经理        |
| 测试与优化阶段         | 第 10-11 周 | 1. 全量功能测试，bug 修复与回归2. 性能测试与优化，内存泄漏排查3. 兼容性测试，主流机型适配4. 安全渗透测试，漏洞修复5. 应用商店审核规范适配 | 测试报告、bug 修复记录、性能优化报告、安全审计报告         | 测试工程师、开发工程师、安全工程师             |
| 灰度发布与正式上线阶段 | 第 12 周    | 1. 内测灰度发布，收集用户反馈2. 问题修复与体验优化3. 应用商店上架资料准备4. 正式上架 App Store、应用宝、华为应用市场等主流应用商店 | 上架应用包、上架资料、用户反馈处理报告、上线总结           | 产品经理、开发工程师、运营专员                 |

### 2. 端到端核心业务实现流程

#### 2.1 新建会话→对话交互全流程

```
1. 用户进入应用，点击新建会话
2. 选择绑定模型、设置系统提示词、配置推理参数、绑定知识库、开启/关闭技能
3. 系统生成唯一session_id，初始化会话实例，持久化到本地存储
4. 用户进入对话界面，输入Query（文本/语音/URL/图片/视频）
5. 对话引擎接收Query，触发当前会话的记忆检索+RAG知识库检索
6. 构造对话Prompt：系统提示词 + 检索到的记忆/RAG内容 + 会话历史上下文 + 当前Query
7. 注入当前会话启用的技能/MCP服务工具定义
8. 送入当前会话绑定的本地/远程模型，启动流式推理
9. 解析模型返回结果，若为工具调用，则调度执行对应技能/MCP服务，将结果返回模型重新生成
10. 流式渲染生成的文本内容到对话界面，同步触发TTS流式播报
11. 对话结束后，将对话内容持久化到当前会话的对话历史表
12. 后台触发记忆提取任务，从对话中提取核心信息，生成当前会话的专属记忆/全局记忆
13. 完成一轮完整对话，等待用户下一轮输入
```

#### 2.2 URL 内容一键总结全流程

```
1. 用户在会话内输入URL，或通过系统分享URL到应用
2. URL解析引擎校验URL合法性，还原短链接，识别平台类型
3. 调度对应平台的解析插件，发起内容请求
4. 提取核心内容：文本内容、图片OCR、音视频ASR转写
5. 内容预处理：过滤广告、无关内容，语义分段，控制token长度
6. 构造总结Prompt，结合用户指定的总结模式（摘要/要点/大纲等）
7. 送入当前会话绑定的大模型，启动流式推理
8. 流式渲染总结内容到对话界面，同步注入当前会话上下文
9. 用户可基于总结内容继续追问，或一键将内容存入知识库
10. 完成总结全流程
```

------

## 第五部分 迭代规划与风险管控

### 1. 版本迭代 Roadmap 更新

| 版本            | 周期     | 核心里程碑                     | 核心交付内容                                                 |
| :-------------- | :------- | :----------------------------- | :----------------------------------------------------------- |
| V1.0 MVP        | 4 周     | 核心基础能力闭环               | 1. 跨平台基础框架搭建2. 本地 / 远程文本模型支持3. 多会话隔离与系统提示词功能4. 基础对话与文本总结功能5. 基础 ASR/TTS 语音对话 |
| V2.0 核心增强   | 4 周     | 核心产品能力对齐 Cherry Studio | 1. 强记忆引擎与 RAG 引擎完整实现2. 多模态图片 / 视频理解能力3. URL 内容总结全平台支持4. Skills 插件系统完整实现5. 实时语音对话全链路优化 |
| V3.0 全功能闭环 | 4 周     | 完整产品能力上线               | 1. MCP 协议完整兼容2. 会话模板、文件夹管理完整实现3. 提示词模板库完整功能4. 全链路性能优化与功耗优化5. 安全合规与应用商店适配完成6. 正式版上架应用商店 |
| V4.0 生态扩展   | 后续迭代 | 生态能力建设                   | 1. Windows/macOS 桌面端支持2. 端到端加密多设备同步3. 技能社区与模型市场4. 团队协作能力5. 插件生态扩展 |

### 2. 核心风险点与解决方案

| 风险点                              | 风险等级 | 根因分析                                     | 解决方案                                                     |
| :---------------------------------- | :------- | :------------------------------------------- | :----------------------------------------------------------- |
| 移动端模型加载 OOM、闪退            | 高       | 模型过大、设备内存不足、内存管理不当         | 1. 优先推荐 4bit/5bit 量化模型，针对移动端优化2. 动态调整模型上下文窗口，根据设备内存自动适配3. 开启 GPU 硬件加速，降低内存占用4. 会话切换时自动卸载非活跃模型，释放内存5. 低内存机型强制限制最大模型参数量 |
| 内容平台 URL 解析规则变更，解析失败 | 中       | 平台反爬策略升级、页面结构变更               | 1. 采用插件化架构，解析规则可热更新，无需发版2. 多套解析方案兜底，主方案失效时自动切换备用方案3. 监控解析成功率，异常时快速响应更新规则 |
| iOS App Store 审核被拒              | 高       | JIT 编译违规、权限申请不合规、内容安全不达标 | 1. llama.cpp 使用 Metal AOT 编译，禁用 JIT2. 权限申请严格遵循苹果规范，明确告知用途3. 内置完善的内容安全过滤机制，严禁违规内容生成4. 提前进行预审，规避审核风险 |
| Android 机型兼容性差                | 中       | CPU 架构、硬件加速、系统版本差异大           | 1. 适配 arm64-v8a/armeabi-v7a 主流架构2. 动态调整推理参数，适配不同性能的机型3. 建立主流机型测试矩阵，全量兼容性测试4. 硬件加速能力自动降级，不支持的机型自动切换 CPU 推理 |
| 实时语音对话延迟高                  | 中       | 串行处理、网络开销、模型推理慢               | 1. 全链路流式处理，ASR→LLM→TTS 全流式交互2. 优先使用本地 ASR/TTS 引擎，降低网络延迟3. 句子级 TTS 合成，边生成边播报，无需等待完整文本4. 优化 VAD 检测，提前识别语音结束，降低等待时间 |