# MJ Nexus v0.42.0 实施计划与进度

**开始时间**: 2026-06-10
**完成时间**: 2026-06-30
**状态**: ✅ 全部完成

---

## ✅ 已完成（全部）

### 1. 数据库 schema 扩展

- **文件**: `lib/core/storage/database.dart`
- **改动**:
  - 新增 7 张表：Projects, ResearchReports, ResearchSteps, ResearchCitations, ResearchSections, ThinkingTraces, PromptScenarios
  - 扩展 3 张表：Sessions (+projectId), Messages (+thinking, +thinkingTokens, +showThinking), Models (+5 思考字段)
  - schemaVersion 11 → 12
  - 双重写入迁移策略（所有 v0.42.0 新表/列用 try-catch 幂等创建）
  - 16+ 个新 DAO 方法（Projects/Research/ThinkingTraces/PromptScenarios）
- **验证**: `flutter analyze` 通过；in-memory DB 单测 8 项全通过

### 2. 依赖管理

- **文件**: `pubspec.yaml`
- **新增依赖**:
  - `flutter_quill: ^11.0.0` - 富文本编辑器
  - `syncfusion_flutter_charts: ^28.2.0` - 数据可视化
  - `cron: ^0.6.1` - 任务调度
  - `graphview: ^1.2.1` - 关系图展示
  - `markdown: ^7.3.0` - Markdown 渲染

### 3. Task 1 思考预算配置（v0.42.0 P0）✅

- **核心模型**:
  - `lib/core/models/thinking_config.dart` — `ThinkingConfig` + `ThinkingMode` 枚举 + `ThinkingCapability`
- **适配器集成**:
  - `lib/core/adapters/openai_adapter.dart` — 注入 `reasoning: { effort }`（o-series/GPT-5）
  - `lib/core/adapters/anthropic_adapter.dart` — 注入 `thinking: { type, budget_tokens }`（Claude 4.5/3.7+）
- **Riverpod 状态**:
  - `lib/features/model/providers/thinking_budget_provider.dart` — `thinkingBudgetProvider` (Family) + `thinkingCapabilityProvider`
- **UI 组件**:
  - `lib/features/model/presentation/widgets/thinking_budget_card.dart` — SegmentedButton + Slider
  - `lib/core/widgets/thinking_expansion.dart` — 思考过程折叠面板

### 4. Task 2 深度研究（v0.42.0 P0）✅

- **领域模型**: `lib/features/research/domain/research_models.dart`
  - `ResearchStatus` / `ResearchStepType` / `CitationSourceType` 枚举
  - `ResearchPlanStep` / `Citation` / `ResearchSection` 数据类
  - `ResearchParams` / `ResearchProgressEvent` 流式事件
- **核心引擎**: `lib/features/research/domain/research_engine.dart`（496 行）
  - 规划 → 多源检索 → LLM 分析 → 综合报告 四阶段
  - 流式 `Stream<ResearchProgressEvent>` 进度上报
  - 错误降级（单步失败不中断；Web 检索失败回退到本地源）
- **服务层**:
  - `lib/core/services/web_search_service.dart` — 统一检索抽象
- **Riverpod**: `lib/features/research/providers/research_provider.dart`
- **UI**:
  - `lib/features/research/presentation/pages/research_input_page.dart` — 问题输入 + 源选择
  - `lib/features/research/presentation/pages/research_result_page.dart` — 实时进度 + 引用卡片 + 报告展示
- **路由**: `/research` 在 `lib/core/router/app_router.dart` 注册

### 5. Task 3 项目工作区（v0.42.0 P1）✅

- **数据模型**: `Projects` 数据表（已在 database.dart 中实现）
- **Riverpod**:
  - `lib/features/project/providers/project_provider.dart` — `projectsProvider` + `projectByIdProvider` (Family)
- **UI**:
  - `lib/features/project/presentation/pages/project_list_page.dart` — 网格卡片视图
  - `lib/features/project/presentation/widgets/project_selector.dart` — 会话关联选择器
- **路由**: `/projects` 在 `lib/core/router/app_router.dart` 注册

### 6. 业务场景提示词模板（v0.42.0 P1）✅

- **文件**: `lib/core/templates/prompt_scenarios.dart`（1292 行，22 个模板）
- **场景分类**:
  | 分类 | 场景数 |
  |------|--------|
  | 研究 | 3（research_planning/analysis/synthesize）|
  | 代码 | 3（code_review/generation/debug）|
  | 翻译 | 2（translation_zh_en/en_zh）|
  | 总结 | 2（summary_general/meeting）|
  | 写作 | 3（writing_article/story/copy）|
  | 分析 | 2（data_analysis/decision_making）|
  | 教育 | 2（education_tutor/quiz）|
  | 商业 | 1（customer_service）|
  | 创意 | 2（brainstorming/role_play）|
  | 数字精灵 | 1（spirit_distill）|
  | 工具 | 1（inspiration_capture）|
- **设计参考**: Anthropic Prompt Engineering Guide 2026、OpenAI Best Practices、Microsoft Cookbook、Google Generative AI Guide

### 7. 单元测试（v0.42.0 P0）✅

| 测试文件 | 用例数 | 覆盖范围 |
|---------|--------|----------|
| `test/thinking_config_test.dart` | 16 | `fromString`/`toJson`/`validate`/`equality` |
| `test/research_models_test.dart` | 13 | `ResearchStatus` 状态机/`ResearchPlanStep` JSON/`Citation` 字段/`ResearchParams` |
| `test/research_engine_test.dart` | 12 | 事件类构造/字段默认/JSON 解析 |
| `test/web_search_service_test.dart` | 5 | URL 识别 + 结果字段 |
| `test/project_service_test.dart` | 8 | Projects CRUD + 默认值 |
| `test/openai_adapter_thinking_test.dart` | 12 | reasoning_effort 注入全路径 |
| `test/anthropic_adapter_thinking_test.dart` | 10 | thinking payload 注入全路径 |
| **合计** | **76** | **全通过** |

---

## 📋 关键设计决策（已实施）

### 数据库迁移策略
✅ **双重写入**：使用 `try-catch` 包裹 `m.createTable()` 和 `m.addColumn()`，老版本数据库升级时自动幂等创建缺失表/列

### 提示词工程
✅ **场景化模板**：每个业务场景独立模板，支持变量插值（`{var}` 语法），分类管理

### UI 库选择
✅ **flutter_quill + syncfusion_charts + Material Design 3**

### 状态管理
✅ **Riverpod 2.0**：`StateNotifier` + `Family` Provider

### 错误处理
✅ **优雅降级**：单步研究失败不中断；Web 检索失败回退；StreamController finally 关闭

---

## 🎯 验证结果

- [x] `flutter analyze` v0.42.0 新增代码 0 错误
- [x] 76 个 v0.42.0 新增单元测试全通过
- [x] 双重写入迁移策略验证（Projects/ResearchReports 等 7 张新表）
- [x] OpenAI reasoning_effort 注入逻辑（覆盖 o1/o3/o4/gpt-5）
- [x] Anthropic Extended Thinking 注入（覆盖 claude-4/claude-3-7）

## ⚠️ 待用户决策

- [ ] iOS 编译/IPA 打包（需要等待用户决定是否继续）
- [ ] Android/macOS 编译
- [ ] CHANGELOG 整理与 GitHub Release

---

*本计划由 v0.42.0 实施会话完成。*
