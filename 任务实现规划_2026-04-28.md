# LLM Studio 任务实现规划

**生成日期：** 2026-04-28  
**任务总数：** 22 项

---

## 一、代码质量修复（优先级 P0）

### 1.1 Deprecated API 修复

| 状态 | 问题 | 位置 | 修复方案 |
|------|------|------|----------|
| ✅ 已修复 | withOpacity | app_theme.dart:160 | → withValues(alpha: 0.06) |
| ⏳ 待修复 | surfaceVariant | quant_level_selector.dart:141 | → surfaceContainerHighest |
| ⏳ 待修复 | groupValue/onChanged | quant_level_selector.dart (Radio) | → 使用 RadioGroup |
| ⏳ 待修复 | groupValue/onChanged | session_detail_page.dart (Radio) | → 使用 RadioGroup |
| ⏳ 待修复 | value → initialValue | session_detail_page.dart:3807 | → initialValue |
| ⏳ 待修复 | alpha 废弃 | manual_page.dart (2处) | → 使用 (*.a * 255.0).round() |
| ⏳ 待修复 | groupValue/onChanged | voice_settings_page.dart (Radio) | → 使用 RadioGroup |
| ⏳ 待修复 | cancelOnError/partialResults/listenMode | asr_service.dart | → SpeechListenOptions |
| ⏳ 待修复 | Parser/evaluate | calculator_skill.dart | → GrammarParser/RealEvaluator |

### 1.2 魔法数字/字符串提取

**当前问题：** 代码中散落大量硬编码数值和字符串

**建议常量类：**
```dart
// lib/core/constants/app_constants.dart
class AppConstants {
  // 动画时长
  static const Duration animationFast = Duration(milliseconds: 150);
  static const Duration animationNormal = Duration(milliseconds: 300);
  static const Duration animationSlow = Duration(milliseconds: 500);
  
  // API 超时
  static const Duration apiTimeoutShort = Duration(seconds: 10);
  static const Duration apiTimeoutNormal = Duration(seconds: 30);
  static const Duration apiTimeoutLong = Duration(seconds: 60);
  
  // 缓存大小
  static const int maxCacheMessages = 100;
  static const int maxImageCacheSize = 50 * 1024 * 1024; // 50MB
  
  // 模型参数
  static const int defaultContextLength = 4096;
  static const int defaultMaxTokens = 2048;
  static const double defaultTemperature = 0.7;
}
```

### 1.3 空安全检查

**高风险位置：**
- `model_inference_engine.dart` - 多处 ? 判断
- `asr_service.dart` - 回调可能为 null
- `tts_service.dart` - 引擎初始化状态

---

## 二、功能增强（优先级 P1）

### 2.1 URL 解析增强（当前 30%）

**目标：** 抖音/小红书/B站 视频+图文解析

**实现方案：**
```
URL 解析服务
├── 抖音解析 (douyin.com, aweme.cn)
│   ├── 提取视频 ID → API 获取元数据
│   ├── 提取封面/标题/作者/点赞数
│   └── 支持无水印视频下载
├── 小红书解析 (xiaohongshu.com, xhsc.cn)
│   ├── 提取笔记 ID → 网页抓取
│   ├── 提取图片列表/标题/正文
│   └── 支持图文和视频
└── B站解析 (bilibili.com)
    ├── 提取 av/BV 号 → API 获取元数据
    ├── 提取封面/标题/UP主/播放量
    └── 支持弹幕和字幕提取
```

**技术选型：**
- 使用 `http` + `html` 包解析 HTML
- 抖音/小红书：无官方 API，需逆向或使用第三方服务
- B站：有开放 API `https://api.bilibili.com/x/web-interface/view`

### 2.2 Skills/MCP 插件完善（当前 40%）

**缺失功能：**
1. MCP 服务器连接管理
2. 工具调度（Tool Calling）
3. MCP 工具结果展示

**实现方案：**
```
MCP 架构
├── MCPConfigPage - 服务器配置 UI
│   ├── 添加/编辑/删除服务器
│   ├── 连接状态显示
│   └── 测试连接功能
├── MCPToolService - 工具调度
│   ├── 工具列表获取
│   ├── 工具调用执行
│   └── 结果格式化
└── MCPToolResultWidget - 结果展示
    ├── JSON 树形展示
    ├── 图片/表格渲染
    └── 复制/分享功能
```

### 2.3 分享扩展（当前 0%）

**实现方案：**

**iOS (Share Extension):**
```dart
// 使用 share_plus 包
import 'package:share_plus/share_plus.dart';

Future<void> shareContent(String text, {List<String>? imagePaths}) async {
  await Share.share(
    text,
    subject: 'LLM Studio 对话分享',
    files: imagePaths?.map((p) => XFile(p)).toList(),
  );
}
```

**Android (Intent):**
```dart
// 同样使用 share_plus，兼容 Android
// 支持：文本/图片/文件 → 分享到任意 App
```

### 2.4 系统提示词模板库 UI

**当前状态：** 无模板库 UI

**实现方案：**
```
提示词模板页面
├── 模板分类
│   ├── 写作助手
│   ├── 编程开发
│   ├── 学习教育
│   ├── 生活助手
│   └── 自定义
├── 模板市场（在线）
│   └── 从服务器获取精选模板
├── 我的模板（本地）
│   └── 用户创建的模板存储
└── 模板编辑
    ├── 名称/分类/描述
    ├── 系统提示词内容
    └── 变量占位符 {{variable}}
```

### 2.5 会话文件夹分类/归档

**当前状态：** 有文件夹功能，但无归档

**实现方案：**
```
会话管理增强
├── 文件夹功能（已有）
│   ├── 创建/重命名/删除
│   └── 移动会话到文件夹
├── 归档功能（新增）
│   ├── 一键归档所有会话
│   ├── 归档恢复
│   └── 自动归档（可选：30天无活动）
└── 批量操作
    ├── 批量移动
    ├── 批量删除
    └── 批量归档
```

### 2.6 实时语音打断 VAD 检测

**当前状态：** 无 VAD，需要长按说话

**实现方案：**
```
VAD 语音活动检测
├── WebRTC VAD (推荐)
│   ├── 轻量级 native 库
│   ├── 支持 8kHz/16kHz/32kHz
│   └── 静音检测阈值可调
├── 语音打断逻辑
│   ├── 检测到语音开始 → AI 停止说话
│   ├── 用户说话结束 → 继续聆听
│   └── 噪声过滤（避免误触发）
└── 配置选项
    ├── 灵敏度（高/中/低）
    ├── 最小语音时长（过滤噪声）
    └── 启用/禁用开关
```

**推荐库：** `flutter_webrtc` 内置 VAD，或使用 `vad` 包

### 2.7 视频理解关键帧提取

**当前状态：** 支持图片理解，不支持视频

**实现方案：**
```
视频理解流程
├── 1. 视频选择
│   └── 从相册/相机选择视频
├── 2. 关键帧提取
│   ├── 均匀采样（每 N 秒取一帧）
│   ├── 场景切换检测（推荐）
│   └── AI 智能选帧（高级）
├── 3. 帧处理
│   ├── 缩放到合适分辨率
│   ├── 质量压缩
│   └── 数量限制（最多 16 帧）
└── 4. 多模态推理
    └── 发送帧序列 + 文字问题
```

**技术选型：**
- `ffmpeg_kit_flutter` - 视频解码和帧提取
- 或使用 `video_player` + `image` 进行帧截图

---

## 三、UI/UX 优化（优先级 P2）

### 3.1 首页引导教程

**实现方案：**
```
引导流程（首次使用）
├── 第1页：欢迎页
│   └── App 介绍 + 开始按钮
├── 第2页：模型下载
│   └── 演示如何下载第一个模型
├── 第3页：创建会话
│   └── 演示如何创建对话
├── 第4页：语音交互
│   └── 演示语音输入和播报
└── 第5页：完成
    └── 进入应用
```

**技术实现：**
- 使用 `flutter_intro_screen` 或自定义 PageView
- 状态存储在 SharedPreferences `has_seen_onboarding`
- 可跳过按钮 + 从设置重新查看

### 3.2 模型选择简化

**当前状态：** 一次性展示所有模型

**实现方案：**
```
三级模型选择
├── Tab1: 推荐
│   ├── 最近使用 (2-3个)
│   └── 系统推荐 (基于场景)
├── Tab2: 热门
│   ├── 下载量排序 Top 10
│   └── 本地可用模型
└── Tab3: 全部
    ├── 分类筛选 (本地/远程/多模态)
    ├── 搜索过滤
    └── 排序 (名称/大小/下载量)
```

### 3.3 语音延迟优化

**问题：** ASR 识别有延迟

**优化方案：**
```
语音延迟优化
├── 1. 本地 ASR 缓存
│   ├── 识别结果本地缓存
│   ├── 上屏同时发送到服务器
│   └── 网络断开时使用本地结果
├── 2. 流式识别
│   ├── 实时上屏（逐字/逐词）
│   └── 减少等待感
└── 3. 界面反馈
    ├── 录音波形动画
    └── 识别中状态提示
```

### 3.4 状态提示增强

**实现方案：**
```
统一状态组件
├── LoadingState
│   ├── 骨架屏（推荐）
│   ├── 加载动画 + 文字
│   └── 进度条（下载/上传）
├── ErrorState
│   ├── 图标 + 错误描述
│   ├── 重试按钮
│   └── 错误码显示
├── SuccessState
│   ├── 操作成功动画
│   └── 可选：自动消失
└── EmptyState
    ├── 图标 + 提示文字
    ├── 操作按钮（如：创建会话）
    └── 可选：引导链接
```

---

## 四、性能优化（优先级 P2）

### 4.1 冷启动优化（3s+ → <1s）

**优化策略：**
```
冷启动优化方案
├── 1. 懒加载
│   ├── 路由懒注册
│   ├── 页面按需加载
│   └── 非核心功能延迟初始化
├── 2. 缓存优化
│   ├── 图片/图标缓存
│   ├── 热点数据预加载
│   └── 共享首选项缓存
├── 3. 启动流程
│   ├── 移除不必要的 init
│   ├── 异步初始化
│   └── 启动画面优化
└── 4. 测量工具
    └── 使用 `app_benchmark` 测量
```

### 4.2 内存占用优化

**优化策略：**
```
内存优化方案
├── 1. 模型量化
│   ├── 推荐 Q4_K_M 量化
│   └── 避免加载过大模型
├── 2. 内存释放
│   ├── 退出对话时释放模型
│   ├── 图片处理后及时释放
│   └── 定期 GC 触发
├── 3. 缓存限制
│   ├── 消息列表虚拟化
│   └── 图片缓存上限
└── 4. 监控工具
    └── 使用 `flutter_memory_profiler`
```

### 4.3 包体积优化（233MB → <150MB）

**优化策略：**
```
包体积优化
├── 1. 插件按需加载
│   ├── 移除未使用的包
│   └── 拆分非核心功能
├── 2. 资源优化
│   ├── 图片 WebP 化
│   ├── 移除调试符号
│   └── 动态下载资源
├── 3. 代码优化
    ├── Tree Shaking
    ├── 混淆压缩
    └── 移除未使用代码
└── 4. 分析工具
    └── `flutter build apk --analyze-size`
```

### 4.4 推理速度优化

**优化策略：**
```
推理速度优化
├── 1. GPU 加速
│   ├── macOS: Metal
│   ├── iOS: Metal
│   ├── Android: Vulkan
│   └── Windows: CUDA
├── 2. 模型优化
│   ├── 使用 GGUF 格式
│   ├── 合理量化等级
│   └── 预加载热模型
├── 3. 缓存优化
│   ├── KV Cache 最大化
│   └── Prompt 缓存
└── 4. 并发优化
    └── 多线程推理（如果支持）
```

---

## 五、稳定性增强（优先级 P3）

### 5.1 崩溃监控 Sentry

**实现方案：**
```yaml
# pubspec.yaml
dependencies:
  sentry_flutter: ^8.0.0

# main.dart
import 'package:sentry_flutter/sentry_flutter.dart';

void main() async {
  await SentryFlutter.init(
    options: dsn: 'YOUR_SENTRY_DSN',
    appRunner: () => runApp(MyApp()),
  );
}
```

**功能：**
- 自动捕获未处理异常
- 用户行为轨迹
- 设备/环境信息
- 自定义标签（用户ID、模型版本）

### 5.2 内存泄漏检查

**实现方案：**
```yaml
# pubspec.yaml
dev_dependencies:
  flutter_leak_tracker: ^1.0.0
```

**检查点：**
- StreamController 未关闭
- Timer 未取消
- 页面未 dispose
- Provider 未 dispose

### 5.3 网络超时重试

**实现方案：**
```dart
// 统一网络请求封装
class NetworkClient {
  final Dio _dio = Dio();
  
  Future<Response> get(
    String url, {
    int maxRetries = 3,
    Duration timeout = Duration(seconds: 30),
  }) async {
    for (int i = 0; i < maxRetries; i++) {
      try {
        return await _dio.get(url).timeout(timeout);
      } on DioException catch (e) {
        if (i == maxRetries - 1) rethrow;
        await Future.delayed(Duration(seconds: i + 1));
      }
    }
    throw Exception('Max retries reached');
  }
}
```

**增强功能：**
- 重试指数退避
- 超时用户提示
- 离线检测 + 提示

---

## 六、任务优先级排序

| 优先级 | 任务 | 预计工作量 |
|--------|------|-----------|
| P0 | Deprecated API 修复 | 2h |
| P1 | URL 解析增强 | 4h |
| P1 | 分享扩展 | 2h |
| P1 | 首页引导教程 | 3h |
| P2 | MCP 插件完善 | 4h |
| P2 | 系统提示词模板库 | 3h |
| P2 | 语音延迟优化 | 2h |
| P2 | 崩溃监控 Sentry | 2h |
| P3 | 其他优化 | 待定 |

---

## 七、下一步行动

**建议分批完成：**

1. **第一批（P0）：** Deprecated API 修复
2. **第二批（P1）：** 分享扩展 + 首页引导
3. **第三批（P2）：** URL 解析 + MCP 完善
4. **第四批（P3）：** 性能优化 + 稳定性

是否需要我开始执行第一批任务（Deprecated API 修复）？